# offline-web-app-sample
Sample offline web app, showing simulation of install/uninstall app.

### Usage

 * Clone repo
 * ```cd``` to project root
 * ``` npm install ```
 * Run with ```npm start``` 
