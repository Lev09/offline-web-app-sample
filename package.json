{
  "name": "sample-offline-webapp",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "live-server"
  },
  "dependencies": {
    "bootstrap": "^3.3.6",
    "live-server": "*"
  }
}
